{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}

    <div class="well">

    </div>
    {{ form_start(form, {action: path('homepage')}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}